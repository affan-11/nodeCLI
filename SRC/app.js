const {addMovie} = require("./utils");

console.log(addMovie({title: "Spiderman", actor: "Andrew Garfield"}));



// Accept at least movie title on command line


// If user input has actor, capture this aswell

// Add movie object to an array

// Output movie/ movies to console